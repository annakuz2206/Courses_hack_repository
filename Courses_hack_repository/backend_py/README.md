# ITAM Hackathon Team Matching - Backend

Backend приложение для платформы поиска команд для хакатонов.

## Возможности

- 🔐 Авторизация через Telegram (два способа):
  - Telegram Login Widget (стандартный способ)
  - Telegram бот с кодами авторизации (новый способ)
- 👥 Управление участниками и командами
- 🔄 Система свайпов и матчей
- 🎫 JWT токены для авторизации

## Установка и запуск

### 1. Установка зависимостей

```bash
cd backend_py
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Создайте файл `.env` в папке `backend_py` или установите переменные окружения:

```bash
# Токен Telegram бота (получите у @BotFather)
export TELEGRAM_BOT_TOKEN="ваш_токен_бота"

# Секретный ключ для JWT (сгенерируйте случайную строку)
export JWT_SECRET="ваш_секретный_ключ"

# URL вашего сайта (для ссылок в боте)
export SITE_URL="http://localhost:3000"  # или ваш production URL
```

**Как получить токен Telegram бота:**
1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Скопируйте токен в переменную `TELEGRAM_BOT_TOKEN`

### 3. Запуск FastAPI сервера

```bash
python main.py
```

Или через uvicorn напрямую:

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

Сервер будет доступен по адресу: `http://localhost:8000`

Документация API (Swagger): `http://localhost:8000/docs`

### 4. Запуск Telegram бота

В **отдельном терминале** запустите бота:

```bash
python telegram_bot.py
```

Бот начнёт принимать команды от пользователей.

## Использование Telegram бота для авторизации

### Для пользователей:

1. Найдите вашего бота в Telegram (имя бота вы задаёте при создании через @BotFather)
2. Отправьте команду `/start` или `/login`
3. Бот отправит вам код авторизации и ссылку на сайт
4. Перейдите по ссылке или скопируйте код
5. На сайте введите код в форму авторизации
6. Вы получите JWT токен для доступа к API

### Команды бота:

- `/start` - Начать работу с ботом
- `/login` - Получить код для авторизации на сайте
- `/help` - Показать справку

## API эндпоинты

### Авторизация

- `POST /api/auth/telegram` - Авторизация через Telegram Login Widget
- `POST /api/auth/bot-code` - Авторизация через код от Telegram бота (новый способ)
- `POST /api/auth/dev-login` - DEV: быстрая авторизация для разработки
- `GET /api/auth/debug-token` - DEV: просмотр содержимого JWT токена

### Участники

- `GET /api/participants` - Список участников (с фильтрами)
- `GET /api/participants/{id}` - Профиль участника
- `GET /api/participants/me` - Мой профиль (требует авторизацию)
- `PUT /api/participants/me` - Обновить мой профиль (требует авторизацию)

### Команды

- `GET /api/teams` - Список команд (с фильтрами)
- `GET /api/teams/{id}` - Информация о команде
- `POST /api/teams` - Создать команду

### Свайпы и матчи

- `POST /api/swipe` - Сделать свайп (right/left)
- `GET /api/matches` - Получить список матчей

## Пример использования API

### Авторизация через код от бота

```bash
# 1. Получите код в Telegram боте командой /login
# 2. Используйте код для авторизации:

curl -X POST http://localhost:8000/api/auth/bot-code \
  -H "Content-Type: application/json" \
  -d '{"code": "ваш_код_от_бота"}'

# Ответ:
# {
#   "access_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
#   "token_type": "bearer"
# }
```

### Использование токена для запросов

```bash
# Получить свой профиль
curl -X GET http://localhost:8000/api/participants/me \
  -H "Authorization: Bearer ваш_jwt_токен"
```

## Структура проекта

```
backend_py/
├── main.py              # FastAPI приложение и API эндпоинты
├── telegram_bot.py      # Telegram бот для авторизации
├── requirements.txt      # Зависимости Python
└── README.md            # Этот файл
```

## Разработка

### Запуск в режиме разработки

FastAPI автоматически перезагружается при изменении кода (благодаря `--reload`).

Для бота нужно перезапускать вручную при изменениях.

### Тестирование

Для тестирования можно использовать:
- Swagger UI: `http://localhost:8000/docs`
- Postman или другой HTTP клиент
- `curl` команды

## Безопасность

⚠️ **Важно для production:**

1. Используйте сильные секретные ключи для `JWT_SECRET`
2. Настройте HTTPS для production
3. Ограничьте время жизни кодов авторизации (сейчас 5 минут)
4. Рассмотрите использование Redis для хранения кодов вместо памяти
5. Добавьте rate limiting для защиты от злоупотреблений

## Troubleshooting

### Бот не запускается

- Проверьте, что `TELEGRAM_BOT_TOKEN` установлен правильно
- Убедитесь, что токен не начинается с "CHANGE_ME_"
- Проверьте интернет-соединение

### Коды авторизации не работают

- Убедитесь, что бот запущен
- Проверьте, что код не истёк (действителен 5 минут)
- Убедитесь, что код не был использован ранее

### Ошибки импорта

- Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
- Проверьте, что используете правильную версию Python (рекомендуется 3.9+)

## Лицензия

Проект создан для хакатона ITAM Courses Hack.

