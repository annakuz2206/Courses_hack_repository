# 📖 Как использовать Swagger UI для тестирования

## Пошаговая инструкция

### Шаг 1: Получите код от Telegram бота

1. Запустите бота: `python3 telegram_bot.py`
2. Отправьте `/login` боту в Telegram
3. Скопируйте код, который пришёл от бота

### Шаг 2: Откройте Swagger UI

Откройте в браузере: http://localhost:8000/docs

### Шаг 3: Авторизуйтесь через код

1. Найдите эндпоинт `POST /api/auth/bot-code`
2. Нажмите "Try it out"
3. Вставьте код в поле:
```json
{
  "code": "ваш_код_от_бота"
}
```
4. Нажмите "Execute"
5. **Скопируйте `access_token` из ответа** - он понадобится для авторизации

**Пример ответа:**
```json
{
  "access_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "token_type": "bearer"
}
```

### Шаг 4: Авторизуйтесь в Swagger UI

1. Нажмите кнопку **"Authorize"** в правом верхнем углу Swagger UI (🔒 иконка)
2. В поле "Value" вставьте ваш `access_token` (БЕЗ слова "Bearer")
3. Нажмите "Authorize"
4. Нажмите "Close"

Теперь вы авторизованы! 🔓

### Шаг 5: Тестируйте защищённые эндпоинты

Теперь вы можете вызывать эндпоинты, которые требуют авторизации:

- `GET /api/auth/debug-token` - посмотреть информацию о токене
- `GET /api/participants/me` - получить свой профиль
- `PUT /api/participants/me` - обновить свой профиль

**Важно:** Если вы видите ошибку 403 Forbidden, значит:
- Вы не авторизовались в Swagger (Шаг 4)
- Или токен истёк (получите новый через `/api/auth/bot-code`)

## Быстрая проверка работы

### Тест 1: Проверка кода (без авторизации)

```
GET /api/auth/bot-code/validate/{code}
```

### Тест 2: Получение токена (без авторизации)

```
POST /api/auth/bot-code
Body: {"code": "ваш_код"}
```

### Тест 3: Проверка токена (ТРЕБУЕТ АВТОРИЗАЦИЮ)

1. Сначала авторизуйтесь (Шаг 4)
2. Затем:
```
GET /api/auth/debug-token
```

## Визуальная инструкция

```
┌─────────────────────────────────────────┐
│  Swagger UI                             │
│                                         │
│  [🔒 Authorize]  ← Нажмите сюда!       │
│                                         │
│  POST /api/auth/bot-code                │
│  └─ Получите токен                      │
│                                         │
│  [🔒 Authorize]  ← Вставьте токен      │
│                                         │
│  GET /api/auth/debug-token              │
│  └─ Теперь работает! ✅                 │
└─────────────────────────────────────────┘
```

## Частые ошибки

### ❌ 403 Forbidden на `/api/auth/debug-token`

**Причина:** Не авторизованы в Swagger UI

**Решение:**
1. Получите токен через `POST /api/auth/bot-code`
2. Нажмите "Authorize" в Swagger
3. Вставьте токен
4. Попробуйте снова

### ❌ 401 Unauthorized

**Причина:** Токен истёк или неверный

**Решение:** Получите новый токен через `POST /api/auth/bot-code`

### ❌ Код недействителен

**Причина:** 
- Код истёк (действителен 5 минут)
- Код уже использован

**Решение:** Получите новый код от бота командой `/login`

## Полезные эндпоинты для тестирования

### Без авторизации:
- `GET /health` - проверка работы сервера
- `GET /api/auth/bot-code/validate/{code}` - проверка кода
- `POST /api/auth/bot-code` - получение токена
- `GET /api/participants` - список участников
- `GET /api/teams` - список команд
- `GET /api/hackathons` - список хакатонов

### С авторизацией:
- `GET /api/auth/debug-token` - информация о токене
- `GET /api/participants/me` - свой профиль
- `PUT /api/participants/me` - обновить профиль
- `POST /api/swipe` - свайп
- `GET /api/matches` - матчи

## Готово! 🎉

Теперь вы знаете, как правильно использовать Swagger UI для тестирования API!

