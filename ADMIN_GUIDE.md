# 🔧 Руководство администратора

## Быстрый старт для организаторов

### 1. Получение прав организатора

**Важно:** Самоназначиться организатором нельзя! Это может сделать только Super Admin.

#### Вариант А: Вы Super Admin

Если вы первый пользователь системы:
1. Войдите с ID: `super_admin_1`
2. У вас уже есть все права

#### Вариант Б: Вас должен назначить Super Admin

Попросите Super Admin'а выполнить:

```javascript
// Super Admin входит в систему
await authAPI.devLogin('super_admin_1');

// Назначает вас организатором (замените 'u1' на ваш ID)
await adminAPI.setOrganizer('u1');
```

Или через Swagger UI (`http://localhost:8000/docs`):
1. Авторизуйтесь как `super_admin_1`
2. Найдите `POST /api/admin/set-organizer`
3. Укажите ваш `participantId`

---

## 2. Вход в админ-панель

### Способ 1: Через Settings
1. Войдите в систему со своим ID
2. Откройте Settings (⚙️)
3. Нажмите кнопку **"🔧 Админ-панель"**

### Способ 2: Прямая ссылка
Перейдите на `http://localhost:5173/admin`

---

## 3. Функции админ-панели

### 📅 Управление хакатонами

**Создание хакатона:**
1. Вкладка "Хакатоны"
2. Кнопка "+ Создать хакатон"
3. Заполните форму:
   - Название
   - Описание
   - Даты начала и окончания
   - Дедлайн регистрации
   - Максимальный размер команды

**Редактирование:**
- Нажмите "Редактировать" на карточке хакатона
- Измените нужные поля
- Сохраните

**Архивирование:**
- Нажмите "Архивировать" на карточке
- Хакатон станет неактивным, но данные сохранятся

---

### 👥 Участники (в разработке)

**Что будет доступно:**
- Таблица всех участников
- Фильтры:
  - С командой / Без команды
  - По роли (Frontend, Backend, Designer...)
  - По хакатону
- Экспорт в CSV для жюри

**Текущий обходной путь:**
Используйте API напрямую:
```
GET /api/participants?hackathonId=h1
GET /api/admin/export/participants?hackathonId=h1
```

---

### 🚀 Команды (в разработке)

**Что будет доступно:**
- Таблица всех команд
- Информация о составе
- Статус укомплектованности
- Экспорт в CSV

**Текущий обходной путь:**
```
GET /api/teams?hackathonId=h1
GET /api/admin/export/teams?hackathonId=h1
```

---

### 🎯 God Mode - Ручное распределение

**Зачем нужно:**
Иногда участники не могут найти команду через свайпы. Организатор может вручную добавить их.

**Как использовать (через API):**

```javascript
// Добавить участника u1 в команду t1
await adminAPI.addToTeam('u1', 't1');
```

**Что происходит:**
1. Участник добавляется в команду
2. Обновляется список участников команды
3. Участнику приходит уведомление (в будущем)

**Через Swagger UI:**
1. `POST /api/admin/add-to-team`
2. Укажите `participantId` и `teamId`

---

### 📊 Аналитика

**Доступная статистика:**

#### Общие метрики
- Всего участников
- Всего команд
- Полных команд (укомплектованы)
- Неполных команд
- Участников без команды
- % укомплектованности

#### Распределение по ролям
Визуальные графики показывают:
- Сколько Frontend, Backend, Designer и т.д.
- Какие роли в дефиците
- Баланс специальностей

#### Размеры команд
- Сколько команд по 2, 3, 4, 5 человек
- Помогает понять, сколько мест еще доступно

#### 💡 Автоматические рекомендации

Система анализирует данные и дает советы:

**"Много участников без команды"**
- Когда: >10 человек без команды
- Действие: Создать пост в канале, организовать networking

**"Много неполных команд"**
- Когда: Неполных команд больше, чем полных
- Действие: Использовать God Mode для распределения

**"Дисбаланс ролей"**
- Когда: Одной роли в 3+ раза больше, чем другой
- Действие: Написать пост "Срочно нужны дизайнеры!"

**"Все команды укомплектованы"**
- Когда: 100% команд готовы
- Действие: Празднуйте! 🎉

---

### 🔐 Управление правами (только Super Admin)

**Назначение организаторов:**

Только Super Admin может давать права организатора другим пользователям.

**Процесс:**
1. Войдите как `super_admin_1`
2. Вкладка "Права"
3. Введите ID участника
4. Нажмите "Назначить организатором"

**Или через API:**
```javascript
await adminAPI.setOrganizer('u5');
```

**Проверка:**
```javascript
const user = await participantsAPI.getById('u5');
console.log(user.userRole); // должно быть "organizer"
```

---

## 4. Типичные сценарии

### Сценарий 1: Подготовка к хакатону

1. **Создайте хакатон** (вкладка "Хакатоны")
2. **Мониторьте регистрации** (вкладка "Аналитика")
3. **Следите за балансом ролей**
4. **За неделю до события:** Проверьте, сколько участников без команды
5. **За 2 дня:** Используйте God Mode для распределения одиночек

### Сценарий 2: Дисбаланс ролей

**Проблема:** 50 бэкендеров, 2 дизайнера

**Решение:**
1. Откройте "Аналитику"
2. Посмотрите "Распределение по ролям"
3. Увидите рекомендацию системы
4. Напишите пост в канал: "Срочно ищем дизайнеров!"
5. Поделитесь ссылкой на регистрацию

### Сценарий 3: Много одиночек

**Проблема:** 20 участников без команды за день до хакатона

**Решение:**
1. Откройте "Участники" (когда будет готово)
2. Фильтр: "Без команды"
3. Откройте "Команды"
4. Фильтр: "Неполные"
5. Используйте God Mode для распределения:
   ```javascript
   // Добавляем Frontend-разработчика в команду, которая ищет Frontend
   await adminAPI.addToTeam('u10', 't5');
   ```

### Сценарий 4: Экспорт для жюри

**Задача:** Отправить список команд жюри

**Решение:**
1. Откройте Swagger UI: `http://localhost:8000/docs`
2. Авторизуйтесь (кнопка "Authorize")
3. `GET /api/admin/export/teams?hackathonId=h1`
4. Скопируйте JSON
5. Конвертируйте в CSV или Excel
6. Отправьте жюри

---

## 5. Полезные команды API

### Проверить свою роль
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  http://localhost:8000/api/participants/me
```

### Получить статистику
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  http://localhost:8000/api/admin/analytics?hackathonId=h1
```

### Экспорт участников
```bash
curl -H "Authorization: Bearer YOUR_TOKEN" \
  http://localhost:8000/api/admin/export/participants?hackathonId=h1
```

---

## 6. Безопасность

### ✅ Правильно

- Назначать организаторов через Super Admin
- Использовать JWT токены
- Проверять права перед действиями
- Логировать важные операции

### ❌ Неправильно

- Давать права организатора всем подряд
- Хранить токены в открытом виде
- Делиться учетными данными Super Admin
- Изменять роли напрямую в базе данных

---

## 7. Troubleshooting

### "У вас нет прав доступа к админ-панели"

**Причина:** Ваша роль не `organizer` или `super_admin`

**Решение:**
1. Проверьте роль: `GET /api/participants/me`
2. Попросите Super Admin назначить вас организатором
3. Перелогиньтесь

### "403 Forbidden" при вызове API

**Причина:** Токен не содержит нужную роль

**Решение:**
1. Выйдите из системы
2. Войдите заново (токен обновится с новой ролью)
3. Повторите запрос

### Не видно кнопки "Админ-панель" в Settings

**Причина:** Роль не загрузилась или не назначена

**Решение:**
1. Обновите страницу
2. Проверьте роль через API
3. Попросите Super Admin назначить права

---

## 8. Контакты и поддержка

**Документация:**
- Полная интеграция: `INTEGRATION.md`
- API Docs: `http://localhost:8000/docs`

**Разработка:**
- Backend: `itam_hack/main.py`
- Frontend: `frontendik/src/components/AdminPanel.jsx`

---

**Удачи в организации хакатонов! 🚀**
