# Структура проекта

## 📁 Файловая структура

```
frontend/
├── src/
│   ├── components/          # Все компоненты
│   │   ├── AuthForm.jsx            # Страница входа
│   │   ├── Questionnaire.jsx       # Заполнение профиля
│   │   ├── RoleSelection.jsx       # Выбор роли (капитан/участник)
│   │   ├── HackathonSelection.jsx  # Выбор хакатона
│   │   ├── TeamSearch.jsx          # Поиск команды/создание
│   │   ├── TeamCreation.jsx        # Форма создания команды
│   │   ├── Feed.jsx                # Свайп-лента (главный экран)
│   │   ├── CardDetails.jsx         # Детали карточки
│   │   ├── MatchList.jsx           # Список матчей
│   │   ├── Chat.jsx                # Чат с командой
│   │   ├── Settings.jsx            # Настройки профиля
│   │   └── AdminPanel.jsx          # Админ-панель
│   │
│   ├── services/
│   │   └── api.js           # API клиент для backend
│   │
│   ├── App.jsx              # Роутинг приложения
│   └── main.jsx             # Точка входа
│
├── index.html               # HTML шаблон
├── package.json             # Зависимости
├── vite.config.js           # Конфигурация Vite
├── .gitignore
└── README.md
```

## 🔄 User Flow

```
1. AuthForm (/)
   ↓
2. Questionnaire (/questionnaire) - если новый пользователь
   ↓
3. RoleSelection (/role-selection)
   ↓
4. HackathonSelection (/hackathon-selection)
   ↓
5. TeamSearch (/team-search)
   ↓
6. Feed (/feed) - главный экран со свайпами
   ├→ CardDetails (/card/:id)
   ├→ MatchList (/matches)
   │   └→ Chat (/chat/:id)
   └→ Settings (/settings)
       └→ AdminPanel (/admin) - только для организаторов
```

## 📋 Компоненты

### 1. AuthForm
**Путь:** `/`
**Функция:** Вход в систему
- Поле ввода ID участника
- Dev-логин для тестирования
- Проверка профиля и редирект

### 2. Questionnaire
**Путь:** `/questionnaire`
**Функция:** Заполнение профиля
- Выбор роли (Frontend, Backend, Designer...)
- Ввод навыков
- О себе
- Опыт хакатонов

### 3. RoleSelection
**Путь:** `/role-selection`
**Функция:** Выбор стратегии
- "Я капитан" - ищу людей в команду
- "Я участник" - ищу команду

### 4. HackathonSelection
**Путь:** `/hackathon-selection`
**Функция:** Выбор хакатона
- Список доступных хакатонов
- Информация о датах и участниках
- Выбор одного хакатона

### 5. TeamSearch
**Путь:** `/team-search`
**Функция:** Выбор действия
- Найти человека
- Создать команду
- Чекбокс "У меня есть команда"

### 6. TeamCreation
**Путь:** `/team-creation`
**Функция:** Создание команды
- Название команды
- Выбор хакатона
- Приглашение участников по username

### 7. Feed
**Путь:** `/feed`
**Функция:** Главный экран - свайп карточек
- Показывает команды (для участника) или участников (для капитана)
- Кнопки лайк/дизлайк
- Уведомление о матчах
- Навигация в Settings и Matches

### 8. CardDetails
**Путь:** `/card/:id`
**Функция:** Детальная информация
- Полная информация о команде/участнике
- Кнопка назад

### 9. MatchList
**Путь:** `/matches`
**Функция:** Список мэтчей
- Все команды, с которыми есть мэтч
- Переход в чат

### 10. Chat
**Путь:** `/chat/:id`
**Функция:** Чат с командой
- Список сообщений
- Форма отправки сообщения

### 11. Settings
**Путь:** `/settings`
**Функция:** Настройки профиля
- Редактирование данных
- Кнопка "Админ-панель" (для организаторов)

### 12. AdminPanel
**Путь:** `/admin`
**Функция:** Админ-панель
- Проверка прав доступа
- Табы: Хакатоны, Участники, Команды, God Mode, Аналитика, Права
- Только для organizer/super_admin

## 🔌 API интеграция

### services/api.js

**authAPI:**
- `devLogin(participantId)` - Dev-вход
- `telegramAuth(data)` - Telegram вход
- `logout()` - Выход

**participantsAPI:**
- `getAll(filters)` - Список участников
- `getById(id)` - Участник по ID
- `getMe()` - Текущий пользователь
- `updateMe(data)` - Обновить профиль

**teamsAPI:**
- `getAll(filters)` - Список команд
- `getById(id)` - Команда по ID
- `create(data)` - Создать команду

**swipeAPI:**
- `swipe(...)` - Свайп
- `getMatches(filters)` - Список мэтчей

**adminAPI:**
- `getHackathons()` - Список хакатонов
- `createHackathon(data)` - Создать хакатон
- `updateHackathon(id, data)` - Обновить
- `deleteHackathon(id)` - Удалить
- `getAnalytics(hackathonId)` - Аналитика
- `setOrganizer(participantId)` - Назначить организатора
- `addToTeam(participantId, teamId)` - God Mode
- `exportParticipants(hackathonId)` - Экспорт
- `exportTeams(hackathonId)` - Экспорт

## 🎨 Добавление стилей

Для каждого компонента создайте CSS файл:

```jsx
// ComponentName.jsx
import './ComponentName.css';

const ComponentName = () => {
  return <div className="component-name">...</div>;
};
```

```css
/* ComponentName.css */
.component-name {
  /* ваши стили */
}
```

## 🚀 Запуск

```bash
# Установка
npm install

# Разработка
npm run dev

# Билд
npm run build

# Превью билда
npm run preview
```

## 🔧 Конфигурация

**vite.config.js:**
- Порт: 5174 (чтобы не конфликтовать с frontendik на 5173)
- React plugin включен

**package.json:**
- React 19
- React Router 7
- Vite 7

## 📝 Примечания

- Все компоненты функциональные (hooks)
- Используется React Router для навигации
- API клиент готов к работе с backend
- Нет стилей - только HTML структура
- Логика работы реализована
- Готово к добавлению CSS/UI библиотек
