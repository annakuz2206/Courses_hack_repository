# Новая структура Frontend приложения

## 📁 Структура проекта

```
frontend/src/
├── context/
│   └── AppContext.jsx          # Глобальный стейт (Context API)
├── pages/
│   ├── AuthPage.jsx            # Страница входа
│   ├── ProfilePage.jsx         # Создание/редактирование профиля
│   ├── HackathonsPage.jsx      # Список хакатонов
│   ├── CreateTeamPage.jsx      # Создание команды
│   ├── SwipePage.jsx           # Tinder-механика (свайп)
│   ├── MatchesPage.jsx         # Список мэтчей
│   └── MyTeamPage.jsx          # Моя команда
├── App.jsx                     # Роутинг
└── main.jsx                    # Точка входа
```

## 🔄 User Flow

1. **AuthPage** (`/`) 
   - Ввод кода от бота
   - Мок-логика: любой код пускает дальше
   - → ProfilePage

2. **ProfilePage** (`/profile`)
   - Имя, Ник (read-only), Роль, Слайдер уверенности, Навыки, О себе
   - БЕЗ галочки "Есть команда"
   - → HackathonsPage

3. **HackathonsPage** (`/hackathons`)
   - Список карточек хакатонов
   - Клик → Модалка с выбором:
     - "Я ищу команду" → SwipePage
     - "Создать команду" → CreateTeamPage

4. **CreateTeamPage** (`/create-team`)
   - Форма: Название команды
   - → MyTeamPage (роль: капитан)

5. **Внутренний интерфейс (3 таба):**
   
   **SwipePage** (`/swipe`)
   - Tinder-механика
   - Свайп влево (пропуск), вправо (лайк)
   
   **MatchesPage** (`/matches`)
   - Список взаимных лайков
   - Кнопка "Связаться"
   
   **MyTeamPage** (`/my-team`)
   - Нет команды → Кнопка "Создать команду"
   - Есть команда → Список участников
   - Капитан может удалять, участник может выйти

## 🎯 Управление состоянием (Context API)

### AppContext предоставляет:

**Состояние:**
- `user` - данные пользователя (id, code, nickname)
- `profile` - профиль (name, role, confidence, skills, bio)
- `selectedHackathon` - выбранный хакатон
- `myTeam` - моя команда (если есть)
- `role` - роль в хакатоне ('searching' или 'captain')

**Функции:**
- `login(code)` - вход в систему
- `logout()` - выход
- `saveProfile(data)` - сохранение профиля
- `selectHackathon(hackathon)` - выбор хакатона
- `selectRole(role)` - выбор роли
- `createTeam(data)` - создание команды
- `leaveTeam()` - выход из команды

### Использование в компонентах:

```javascript
import { useApp } from '../context/AppContext';

const MyComponent = () => {
  const { user, profile, saveProfile } = useApp();
  
  // Используем данные и функции
  console.log(user.nickname);
  saveProfile({ name: 'Иван', ... });
};
```

## 🎨 Дизайн

- Строго черно-белый
- Без эмоджи (кроме иконок хакатонов)
- Границы: 2px solid #000000
- Оптимизация под iPhone 13 mini (375px)
- Минимальная высота кнопок: 44px (iOS tap target)

## 📝 Комментарии в коде

Все функции и блоки прокомментированы на русском языке для понимания логики.

## 🚀 Следующие шаги

1. ✅ Создана структура папок
2. ✅ Реализован Context API
3. ✅ Созданы все страницы
4. ✅ Настроен роутинг
5. ⏳ Подключение к бэкенду (следующий этап)
6. ⏳ Добавление Tailwind CSS (опционально)
7. ⏳ Тестирование flow
