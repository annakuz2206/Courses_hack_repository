# 🎯 Руководство по админ-панели для организаторов

## 📋 Обзор

Админ-панель предоставляет организаторам полный контроль над хакатонами, участниками и командами.

## 🔐 Доступ к админ-панели

### URL
```
http://localhost:5174/admin
```

### Авторизация
Для доступа к админ-панели нужны права организатора. Используйте один из способов:

**1. Dev-режим (для разработки):**
```javascript
// Войдите как super_admin_1 или пользователь с ролью admin
// На странице входа введите: super_admin_1
```

**2. Production:**
- Войдите через Telegram бота
- Попросите супер-админа назначить вас организатором

## 📊 Функционал

### 1. Управление хакатонами

#### Создание хакатона
1. Перейдите на вкладку "Хакатоны"
2. Нажмите "+ Создать хакатон"
3. Заполните форму:
   - **Название**: Название хакатона
   - **Описание**: Подробное описание
   - **Локация**: Место проведения
   - **Макс. размер команды**: Максимальное количество участников в команде
   - **Статус**: 
     - `upcoming` - Предстоящий
     - `active` - Активный
     - `finished` - Завершённый
4. Нажмите "Сохранить"

#### Редактирование хакатона
1. Найдите карточку хакатона
2. Нажмите "Редактировать"
3. Измените нужные поля
4. Сохраните изменения

#### Пример данных:
```json
{
  "name": "AI Hackathon 2025",
  "description": "Хакатон по искусственному интеллекту",
  "location": "Москва, ITAM Campus",
  "maxTeamSize": 5,
  "status": "active"
}
```

### 2. Аналитика

Вкладка "Аналитика" показывает:

#### Основные метрики
- **Всего участников**: Общее количество зарегистрированных
- **Всего команд**: Количество созданных команд
- **Без команды**: Участники, не вступившие в команду
- **Полных команд**: Команды, достигшие максимального размера

#### Экспорт данных
- **Экспорт участников (CSV)**: Скачать список всех участников
- **Экспорт команд (CSV)**: Скачать список всех команд

**Формат CSV участников:**
```csv
id,name,age,role,skills,bio,hackathonId,experienceHackathons
u1,Аня,21,Frontend,"React,TypeScript",Ищу команду...,h1,2
```

**Формат CSV команд:**
```csv
id,name,hackathonId,captainId,lookingForRoles,description
t1,AI Ninjas,h1,u2,"Frontend,Designer",Делаем AI-сервис...
```

### 3. Просмотр участников

Вкладка "Участники" показывает таблицу со всеми участниками выбранного хакатона:

| Имя | Роль | Навыки | Опыт | Команда |
|-----|------|--------|------|---------|
| Аня | Frontend | React, TypeScript | 2 | Нет |
| Илья | Backend | Node.js, Python | 3 | t1 |

**Функции:**
- Просмотр всех участников
- Фильтрация по хакатону (выпадающий список вверху)
- Экспорт в CSV

### 4. Просмотр команд

Вкладка "Команды" показывает таблицу со всеми командами:

| Название | Капитан | Ищут роли | Описание |
|----------|---------|-----------|----------|
| AI Ninjas | u2 | Frontend, Designer | Делаем AI-сервис... |

**Функции:**
- Просмотр всех команд
- Фильтрация по хакатону
- Экспорт в CSV

### 5. Ручное распределение участников

**API готово, UI в разработке:**

```javascript
// Добавить участника в команду вручную
await adminAPI.addToTeam('u1', 't1');
```

Эта функция будет добавлена в следующей версии с drag-n-drop интерфейсом.

## 🎨 Интерфейс

### Навигация
- **Выпадающий список хакатонов**: Переключение между хакатонами
- **Кнопка "Выход"**: Выход из админ-панели

### Табы
1. **Хакатоны** - Управление хакатонами
2. **Аналитика** - Статистика и экспорт
3. **Участники** - Таблица участников
4. **Команды** - Таблица команд

## 📱 Адаптивность

Админ-панель оптимизирована для:
- **Desktop**: ≥1280px (рекомендуется)
- **Tablet**: 768px - 1279px
- **Mobile**: <768px (ограниченный функционал)

## 🔧 API эндпоинты

Все эндпоинты требуют JWT токен с ролью `admin` или `organizer`:

```javascript
// Хакатоны
GET    /api/hackathons              // Список хакатонов
POST   /api/admin/hackathons        // Создать хакатон
PUT    /api/admin/hackathons/{id}   // Обновить хакатон
DELETE /api/admin/hackathons/{id}   // Удалить хакатон

// Аналитика
GET /api/admin/analytics?hackathonId=h1

// Экспорт
GET /api/admin/export/participants?hackathonId=h1
GET /api/admin/export/teams?hackathonId=h1

// Ручное распределение
POST /api/admin/add-to-team
Body: { "participantId": "u1", "teamId": "t1" }
```

## 🚀 Быстрый старт

### 1. Запустите проект
```bash
# Backend
cd Courses_hack_repository/backend_py
python main.py

# Frontend
cd frontendik
npm run dev
```

### 2. Войдите как админ
```
URL: http://localhost:5174
Введите: super_admin_1
```

### 3. Перейдите в админ-панель
```
URL: http://localhost:5174/admin
```

### 4. Создайте хакатон
1. Вкладка "Хакатоны"
2. "+ Создать хакатон"
3. Заполните форму
4. Сохраните

### 5. Просмотрите аналитику
1. Вкладка "Аналитика"
2. Выберите хакатон
3. Посмотрите статистику
4. Экспортируйте данные

## 📊 Примеры использования

### Создание хакатона
```javascript
const newHackathon = {
    name: "AI Challenge 2025",
    description: "Хакатон по искусственному интеллекту",
    location: "Москва",
    maxTeamSize: 4,
    status: "upcoming"
};

await adminAPI.createHackathon(newHackathon);
```

### Получение аналитики
```javascript
const analytics = await adminAPI.getAnalytics('h1');

console.log(`Участников: ${analytics.totalParticipants}`);
console.log(`Команд: ${analytics.totalTeams}`);
console.log(`Без команды: ${analytics.participantsWithoutTeam}`);
```

### Экспорт данных
```javascript
// Экспорт участников
const participants = await adminAPI.exportParticipants('h1');
// Скачивается CSV файл

// Экспорт команд
const teams = await adminAPI.exportTeams('h1');
// Скачивается CSV файл
```

## 🔒 Безопасность

### Проверка прав
Все админские эндпоинты проверяют роль пользователя:

```javascript
// В бэкенде
def get_current_admin(credentials):
    payload = jwt.decode(token)
    role = payload.get('role')
    
    if role != 'admin':
        raise HTTPException(403, "Доступ запрещён")
```

### Назначение организаторов
Только супер-админ может назначать организаторов:

```javascript
// Только для super_admin_1
await adminAPI.setOrganizer('u1');
```

## 🐛 Troubleshooting

### Нет доступа к админ-панели
**Проблема**: 403 Forbidden
**Решение**: 
1. Проверьте роль: `await authAPI.debugToken()`
2. Войдите как `super_admin_1`
3. Попросите админа назначить вас организатором

### Не загружаются данные
**Проблема**: Пустые таблицы
**Решение**:
1. Проверьте, что выбран правильный хакатон
2. Убедитесь, что есть участники/команды
3. Проверьте консоль браузера (F12)

### Ошибка при экспорте
**Проблема**: Не скачивается CSV
**Решение**:
1. Проверьте, что есть данные для экспорта
2. Проверьте права доступа
3. Попробуйте другой браузер

## 📚 Дополнительные ресурсы

- **Backend API**: http://localhost:8000/docs
- **Swagger документация**: Полное описание всех эндпоинтов
- **INTEGRATION_GUIDE.md**: Руководство по интеграции
- **QUICK_START.md**: Быстрый старт

## 🎯 Roadmap

### В разработке
- [ ] Drag-n-drop для ручного распределения
- [ ] Фильтры и поиск в таблицах
- [ ] Графики и визуализация данных
- [ ] Уведомления в реальном времени
- [ ] Массовые операции
- [ ] История изменений

### Планируется
- [ ] Email уведомления участникам
- [ ] Экспорт в Excel
- [ ] Импорт участников из CSV
- [ ] Шаблоны хакатонов
- [ ] Роли и права доступа

---

**Версия**: 1.0.0  
**Дата**: Декабрь 2025  
**Статус**: ✅ Готово к использованию
