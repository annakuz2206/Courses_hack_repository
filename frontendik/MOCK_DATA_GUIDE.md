# 📦 Гайд по моковым данным

## Описание

Приложение работает **полностью автономно** без бэкенда. Все данные хранятся локально в браузере (localStorage) и в памяти приложения.

## Структура данных

### 1. Моковые хакатоны (`mockHackathons`)

Файл: `src/data/mockData.js`

```javascript
{
  id: 'h1',
  name: 'ITAM Hackathon 2025',
  date: '5-7 декабря 2025',
  emoji: '🎯',
  description: 'Описание хакатона',
  isActive: true, // Можно ли выбрать
}
```

### 2. Моковые участники (`mockParticipants`)

8 участников с разными ролями:

```javascript
{
  id: 'p1',
  name: 'Аня',
  role: 'Frontend разработчик',
  confidence: 80, // Уверенность в навыках (0-100)
  skills: ['React', 'TypeScript', 'CSS'],
  bio: 'Описание участника',
}
```

### 3. Роли (`mockRoles`)

Список доступных ролей для выбора в профиле:
- Frontend разработчик
- Backend разработчик
- Дизайнер
- Mobile разработчик
- QA инженер
- Менеджер
- Data Scientist
- DevOps
- ML Engineer

## Как работает стейт

### localStorage

Приложение сохраняет данные в localStorage:

```javascript
// Пользователь
localStorage.setItem('user', JSON.stringify({
  id: 'test123',
  code: 'test123',
  nickname: 'user_test123',
  token: 'fake_token_...'
}));

// Профиль
localStorage.setItem('profile', JSON.stringify({
  name: 'Иван',
  role: 'Frontend разработчик',
  confidence: 80,
  skills: 'React, TypeScript',
  bio: 'Описание...'
}));

// Выбранный хакатон
localStorage.setItem('selectedHackathon', JSON.stringify({...}));

// Моя команда
localStorage.setItem('myTeam', JSON.stringify({
  id: 'team_123',
  name: 'Code Warriors',
  captain: 'test123',
  members: [...]
}));

// Роль (searching или captain)
localStorage.setItem('role', 'captain');

// Свайпы
localStorage.setItem('swipes', JSON.stringify([
  { participantId: 'p1', direction: 'right', timestamp: 123 }
]));

// Мэтчи
localStorage.setItem('matches', JSON.stringify([
  { id: 'p1', timestamp: 123 }
]));
```

### Context API

Все данные управляются через `AppContext`:

```javascript
const {
  user,              // Текущий пользователь
  profile,           // Профиль пользователя
  selectedHackathon, // Выбранный хакатон
  myTeam,            // Моя команда
  role,              // Роль (searching/captain)
  swipes,            // История свайпов
  matches,           // Список мэтчей
  
  // Функции
  login,             // Вход (фейковый)
  saveProfile,       // Сохранение профиля
  selectHackathon,   // Выбор хакатона
  createTeam,        // Создание команды
  addSwipe,          // Добавление свайпа
  addMatch,          // Добавление мэтча
  leaveTeam,         // Выход из команды
  logout,            // Выход из аккаунта
} = useApp();
```

## Логика работы

### 1. Авторизация

```javascript
// Любой код пускает дальше
login('test123');
// Создается фейковый токен и сохраняется в localStorage
```

### 2. Свайпы

```javascript
// При свайпе вправо
addSwipe('p1', 'right');

// С вероятностью 30% создается мэтч
if (Math.random() > 0.7) {
  addMatch('p1');
  alert('🎉 Мэтч!');
}
```

### 3. Фильтрация участников

```javascript
// Показываем только тех, кого еще не свайпали
const swipedIds = swipes.map(s => s.participantId);
const notSwipedYet = mockParticipants.filter(p => !swipedIds.includes(p.id));
```

### 4. Создание команды

```javascript
createTeam({ name: 'Code Warriors' });
// Автоматически:
// - Создается ID команды
// - Пользователь становится капитаном
// - Роль меняется на 'captain'
```

## Добавление новых данных

### Добавить участника

Отредактируй `src/data/mockData.js`:

```javascript
export const mockParticipants = [
  // ... существующие
  {
    id: 'p9',
    name: 'Новый участник',
    role: 'Backend разработчик',
    confidence: 75,
    skills: ['Python', 'Django', 'PostgreSQL'],
    bio: 'Описание нового участника',
  },
];
```

### Добавить хакатон

```javascript
export const mockHackathons = [
  // ... существующие
  {
    id: 'h4',
    name: 'Новый хакатон',
    date: '1-3 марта 2026',
    emoji: '🚀',
    description: 'Описание нового хакатона',
    isActive: true,
  },
];
```

### Добавить роль

```javascript
export const mockRoles = [
  // ... существующие
  'Новая роль',
];
```

## Сброс данных

Чтобы очистить все данные:

```javascript
// В консоли браузера
localStorage.clear();
window.location.reload();
```

Или через кнопку "Выход" в приложении (если добавить).

## Тестирование

### Сценарий 1: Поиск команды

1. Войти с кодом `test1`
2. Заполнить профиль
3. Выбрать хакатон → "Я ищу команду"
4. Свайпать участников
5. Проверить мэтчи

### Сценарий 2: Создание команды

1. Войти с кодом `test2`
2. Заполнить профиль
3. Выбрать хакатон → "Создать команду"
4. Ввести название команды
5. Проверить страницу "Моя команда"

### Сценарий 3: Повторный вход

1. Войти с кодом `test3`
2. Заполнить профиль
3. Обновить страницу
4. Войти с тем же кодом `test3`
5. Профиль должен сохраниться

## Ограничения

1. **Нет персистентности между устройствами** - данные хранятся только в браузере
2. **Нет реальных мэтчей** - мэтчи создаются случайно (30% вероятность)
3. **Нет чата** - кнопка "Связаться" показывает alert
4. **Нет валидации** - можно создать несколько команд с одним названием
5. **Нет поиска** - показываются все участники по очереди

## Будущие улучшения

Когда появится бэкенд:

1. Заменить `mockData.js` на реальные API запросы
2. Добавить `fetch` в `AppContext`
3. Убрать `localStorage` для данных (оставить только токен)
4. Добавить реальную логику мэтчей
5. Добавить WebSocket для чата
