# 🎨 Гайд по дизайну приложения

## Цветовая палитра

### Основные цвета
- **Фон**: `#000000` (черный)
- **Текст**: `#FFFFFF` (белый)
- **Градиент основной**: Purple (#8B5CF6) → Pink (#F472B6)

### Акцентные цвета
- **Purple**: `#8B5CF6` (основной фиолетовый)
- **Pink**: `#F472B6` (розовый акцент)
- **Orange**: `#FB923C` (оранжевый акцент)

### Прозрачности
- Стекло: `bg-white/5` (5% белого)
- Границы: `border-white/10` (10% белого)
- Текст вторичный: `text-white/60` (60% белого)
- Текст третичный: `text-white/40` (40% белого)

## Компоненты

### 1. Glass Card (Стеклянная карточка)
```jsx
<div className="glass-card p-6">
  {/* Контент */}
</div>
```
- Полупрозрачный фон с размытием
- Тонкая белая граница
- Скругленные углы (rounded-3xl)
- Тень

### 2. Gradient Button (Градиентная кнопка)
```jsx
<button className="btn-gradient w-full">
  Текст кнопки
</button>
```
- Градиент от фиолетового к розовому
- Анимация при наведении (scale)
- Тень с фиолетовым свечением
- Минимальная высота 44px (iOS tap target)

### 3. Input Field (Поле ввода)
```jsx
<input 
  type="text"
  className="input-field"
  placeholder="Введите текст"
/>
```
- Полупрозрачный фон
- Фокус с фиолетовой обводкой
- Плавные переходы

### 4. Gradient Blur Background (Размытый градиентный фон)
```jsx
<div className="gradient-blur-bg">
  {/* Контент */}
</div>
```
- Анимированные градиентные пятна
- Размытие 60px
- Создает эффект глубины

## Иконки (Lucide React)

Используемые иконки:
- `User` - профиль, пользователь
- `Briefcase` - роль, работа
- `Award` - навыки, достижения
- `FileText` - описание, текст
- `Calendar` - даты
- `Users` - команда
- `Search` - поиск
- `Heart` - лайк, мэтчи
- `X` - закрыть, отклонить
- `Crown` - капитан
- `MessageCircle` - сообщения
- `ArrowLeft` - назад
- `LogOut` - выход

## Анимации

### Hover эффекты
- Кнопки: `hover:scale-105` (увеличение на 5%)
- Карточки: `hover:scale-105`
- Текст: `hover:text-white` (изменение цвета)

### Active эффекты
- Кнопки: `active:scale-95` (уменьшение на 5%)
- Создает тактильный отклик

### Transitions
- Все переходы: `transition-all duration-300`
- Плавные анимации для лучшего UX

## Типографика

### Заголовки
- H1: `text-3xl font-bold` (30px)
- H2: `text-2xl font-bold` (24px)
- H3: `text-xl font-bold` (20px)

### Текст
- Основной: `text-base` (16px)
- Маленький: `text-sm` (14px)
- Очень маленький: `text-xs` (12px)

### Веса
- Bold: `font-bold` (700)
- Semibold: `font-semibold` (600)
- Normal: `font-normal` (400)

## Отступы и размеры

### Padding
- Маленький: `p-4` (16px)
- Средний: `p-6` (24px)
- Большой: `p-8` (32px)

### Gap (расстояние между элементами)
- Маленький: `gap-2` (8px)
- Средний: `gap-4` (16px)
- Большой: `gap-6` (24px)

### Скругления
- Маленькое: `rounded-xl` (12px)
- Среднее: `rounded-2xl` (16px)
- Большое: `rounded-3xl` (24px)
- Полное: `rounded-full` (50%)

## Адаптивность

### Максимальная ширина
- Все страницы: `max-w-[375px]` (iPhone 13 mini)
- Центрирование: `mx-auto`

### Минимальные размеры кнопок
- Высота: `min-h-[44px]` (iOS tap target)
- Padding: `px-8 py-4`

## Примеры использования

### Страница с формой
```jsx
<div className="min-h-screen bg-black gradient-blur-bg py-8 px-4">
  <div className="max-w-[375px] mx-auto">
    <h1 className="text-3xl font-bold text-white mb-8 text-center">
      Заголовок
    </h1>
    
    <div className="glass-card p-6">
      <input type="text" className="input-field" />
    </div>
    
    <button className="btn-gradient w-full mt-6">
      Отправить
    </button>
  </div>
</div>
```

### Карточка участника
```jsx
<div className="glass-card p-6">
  <h3 className="text-xl font-bold text-white mb-2">
    Имя
  </h3>
  <p className="text-purple-400 mb-4">
    Роль
  </p>
  <div className="flex flex-wrap gap-2">
    <span className="px-3 py-1 text-xs border border-purple-500/30 rounded-full text-purple-300">
      Навык
    </span>
  </div>
</div>
```

## Советы

1. **Всегда используй glass-card** для контейнеров с контентом
2. **Градиентные кнопки** для основных действий
3. **Иконки** добавляют визуальную иерархию
4. **Анимации** делают интерфейс живым
5. **Прозрачности** создают глубину
6. **Минимальные размеры** для мобильных устройств

## Запуск проекта

```bash
cd frontend
npm run dev
```

Откройте http://localhost:5175/
