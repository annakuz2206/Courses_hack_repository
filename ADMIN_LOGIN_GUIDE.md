# Инструкция по входу в админ-панель

## Вход через dev-login

Для входа в админ-панель используйте специальный ID администратора:

```
super_admin_1
```

## Шаги для входа:

1. Откройте страницу авторизации: http://localhost:5174
2. В поле "Код от бота" введите: `super_admin_1`
3. Нажмите кнопку "Войти"
4. Вы будете автоматически перенаправлены на админ-панель: http://localhost:5174/admin

## Другие тестовые пользователи:

- `u1` - Аня (Frontend разработчик)
- `u2` - Илья (Backend разработчик)
- `u3` - Катя (Designer)
- `u4` - Дима (ML Engineer)

## Что изменилось:

1. ✅ Добавлен пользователь `super_admin_1` с ролью "admin" в базу данных
2. ✅ Исправлен `dev-login` endpoint - теперь передает правильную роль в JWT
3. ✅ Добавлена функция `getUserRole()` в API для проверки роли пользователя
4. ✅ Обновлена логика авторизации - админы автоматически редиректятся на `/admin`
5. ✅ Создан роутер `/api/admin` с эндпоинтами для аналитики и экспорта

## Доступные функции админ-панели:

- 📊 Просмотр аналитики (участники, команды, статистика)
- 📥 Экспорт данных в CSV (участники и команды)
- ✏️ Создание и редактирование хакатонов
- 👥 Просмотр списка участников
- 🏆 Просмотр списка команд

## Проверка работы:

1. Войдите как `super_admin_1`
2. Убедитесь, что открылась админ-панель (не iPhone контейнер, а полный экран)
3. Выберите хакатон из выпадающего списка
4. Перейдите на вкладку "Аналитика" - должны загрузиться 4 метрики
5. Попробуйте экспортировать данные в CSV

## Если что-то не работает:

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Проверьте, что бэкенд запущен: http://localhost:8000/docs
3. Проверьте, что фронтенд запущен: http://localhost:5174
4. Очистите localStorage и попробуйте войти снова
