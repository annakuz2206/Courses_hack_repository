# 🚀 Приложение без бэкенда (Mock Data)

## ✅ Что исправлено

Убраны все запросы к API (`fetch`, `axios`). Приложение работает **полностью автономно** в браузере.

## 📦 Как это работает

### 1. Моковые данные
Все данные находятся в `src/data/mockData.js`:
- 3 хакатона
- 8 участников для свайпа
- 9 ролей для выбора

### 2. Локальное хранилище
Данные сохраняются в `localStorage`:
- Профиль пользователя
- Выбранный хакатон
- Моя команда
- История свайпов
- Список мэтчей

### 3. Context API
Управление состоянием через `AppContext` (без Redux/Zustand).

## 🎮 Как тестировать

### Запуск
```bash
cd frontend
npm run dev
```

Откройте: **http://localhost:5175/**

### Тестовый сценарий

1. **Вход**
   - Введите любой код (например: `test123`)
   - Нажмите "Войти"

2. **Профиль**
   - Заполните имя: `Иван`
   - Выберите роль: `Frontend разработчик`
   - Установите уверенность: `80%`
   - Навыки: `React, TypeScript, CSS`
   - О себе: `Люблю создавать интерфейсы`
   - Нажмите "Сохранить"

3. **Выбор хакатона**
   - Кликните на "ITAM Hackathon 2025"
   - В модалке выберите "Я ищу команду"

4. **Свайп**
   - Свайпайте участников влево (пропуск) или вправо (лайк)
   - При лайке с вероятностью 30% появится мэтч
   - Счетчик показывает прогресс (1/8, 2/8...)

5. **Мэтчи**
   - Перейдите на таб "Мэтчи"
   - Увидите список людей, с которыми произошел мэтч
   - Нажмите "Связаться" (пока показывает alert)

6. **Моя команда**
   - Перейдите на таб "Моя команда"
   - Нажмите "Создать команду"
   - Введите название: `Code Warriors`
   - Увидите себя как капитана

7. **Повторный вход**
   - Обновите страницу (F5)
   - Войдите с тем же кодом `test123`
   - Профиль и команда сохранятся!

## 🔧 Что можно настроить

### Добавить участников

Отредактируйте `src/data/mockData.js`:

```javascript
export const mockParticipants = [
  // ... существующие
  {
    id: 'p9',
    name: 'Ваше имя',
    role: 'Ваша роль',
    confidence: 85,
    skills: ['Skill1', 'Skill2'],
    bio: 'Ваше описание',
  },
];
```

### Изменить вероятность мэтча

В `src/pages/SwipePage.jsx` найдите:

```javascript
// Сейчас: 30% вероятность (Math.random() > 0.7)
if (direction === 'right' && Math.random() > 0.7) {
  // Изменить на 50%: Math.random() > 0.5
  // Изменить на 100%: true
}
```

### Добавить хакатон

В `src/data/mockData.js`:

```javascript
export const mockHackathons = [
  // ... существующие
  {
    id: 'h4',
    name: 'Ваш хакатон',
    date: '1-3 марта 2026',
    emoji: '🚀',
    description: 'Описание',
    isActive: true,
  },
];
```

## 🐛 Решение проблем

### Ошибка "500 Internal Server Error"
✅ **Исправлено!** Все запросы к API удалены.

### Данные не сохраняются
Проверьте, что localStorage не заблокирован в браузере:
```javascript
// В консоли браузера
console.log(localStorage.getItem('user'));
```

### Участники не показываются
Проверьте `src/data/mockData.js` - там должны быть участники.

### Мэтчи не появляются
Это нормально! Мэтчи создаются случайно (30% вероятность). Свайпните вправо несколько раз.

## 📁 Структура файлов

```
frontend/src/
├── data/
│   └── mockData.js          # Моковые данные (хакатоны, участники)
├── context/
│   └── AppContext.jsx       # Управление состоянием (без API)
├── pages/
│   ├── AuthPage.jsx         # Вход (фейковый)
│   ├── ProfilePage.jsx      # Профиль
│   ├── HackathonsPage.jsx   # Список хакатонов
│   ├── SwipePage.jsx        # Свайп (с моковыми данными)
│   ├── MatchesPage.jsx      # Мэтчи (из localStorage)
│   ├── MyTeamPage.jsx       # Моя команда
│   └── CreateTeamPage.jsx   # Создание команды
└── App.jsx                  # Роутинг
```

## 🎯 Что работает

✅ Вход с любым кодом  
✅ Создание и сохранение профиля  
✅ Выбор хакатона  
✅ Свайп участников (8 человек)  
✅ Создание мэтчей (случайно)  
✅ Просмотр мэтчей  
✅ Создание команды  
✅ Управление командой (капитан)  
✅ Сохранение данных в localStorage  
✅ Повторный вход с сохранением данных  

## 🚫 Что НЕ работает (пока нет бэкенда)

❌ Реальные мэтчи (только случайные)  
❌ Чат с участниками  
❌ Синхронизация между устройствами  
❌ Поиск по фильтрам  
❌ Уведомления  

## 🔮 Когда появится бэкенд

Нужно будет:
1. Заменить `mockData.js` на API запросы
2. Добавить `fetch` в `AppContext`
3. Убрать `localStorage` для данных (оставить только токен)
4. Добавить реальную логику мэтчей
5. Добавить WebSocket для чата

Но структура приложения останется той же!

## 📚 Документация

- `DESIGN_GUIDE.md` - Гайд по дизайну и стилям
- `MOCK_DATA_GUIDE.md` - Подробно про моковые данные
- `NEW_STRUCTURE.md` - Структура проекта

## 🎉 Готово!

Приложение работает полностью автономно без бэкенда. Все данные хранятся локально в браузере.

Протестируйте и наслаждайтесь! 🚀
