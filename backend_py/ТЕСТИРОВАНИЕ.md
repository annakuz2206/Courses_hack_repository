# 🧪 Тестирование авторизации через Telegram бота

## Пошаговая инструкция для тестирования через Swagger

### Шаг 1: Запустите FastAPI сервер

В первом терминале:

```bash
cd backend_py
export JWT_SECRET="balls"
python3 main.py
```

Или через uvicorn:

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

Сервер должен быть доступен на: `http://localhost:8000`

### Шаг 2: Запустите Telegram бота

Во втором терминале:

```bash
cd backend_py
export TELEGRAM_BOT_TOKEN="8258716483:AAEx-7BxalTtRH-TkWW7PoADS0K8JHuUY_k"
export SITE_URL="http://localhost:3000"
python3 telegram_bot.py
```

### Шаг 3: Получите код от бота

1. Откройте Telegram
2. Найдите вашего бота (`tuff_af_hack_bot`)
3. Отправьте команду `/login`
4. Скопируйте код, который пришёл от бота (например: `3xyezbpbr-vuqu5ncfh87w`)

**Важно:** Код действителен только 5 минут!

### Шаг 4: Откройте Swagger UI

Откройте в браузере: `http://localhost:8000/docs`

### Шаг 5: Протестируйте эндпоинт `/api/auth/bot-code`

1. Найдите эндпоинт `POST /api/auth/bot-code` в списке
2. Нажмите на него, чтобы развернуть
3. Нажмите кнопку **"Try it out"**
4. В поле `code` введите код, который получили от бота:

```json
{
  "code": "ваш_код_от_бота"
}
```

5. Нажмите кнопку **"Execute"**

### Шаг 6: Проверьте результат

**Успешный ответ (200):**

```json
{
  "access_token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
  "token_type": "bearer"
}
```

**Ошибка (401):**

```json
{
  "detail": "Код недействителен, истёк или уже использован"
}
```

### Шаг 7: Используйте полученный токен

1. Скопируйте `access_token` из ответа
2. Найдите любой защищённый эндпоинт (например, `GET /api/participants/me`)
3. Нажмите кнопку **"Authorize"** вверху страницы Swagger
4. Вставьте токен в поле (без слова "Bearer", только сам токен)
5. Или вставьте в формате: `Bearer ваш_токен`
6. Нажмите **"Authorize"** и **"Close"**
7. Теперь все запросы будут отправляться с этим токеном

### Шаг 8: Проверьте защищённый эндпоинт

1. Найдите `GET /api/participants/me`
2. Нажмите **"Try it out"**
3. Нажмите **"Execute"**
4. Должен вернуться ваш профиль участника

## 🔍 Отладка

### Проверить содержимое токена

Используйте эндпоинт `GET /api/auth/debug-token`:

1. Авторизуйтесь в Swagger (кнопка "Authorize" вверху)
2. Вставьте токен
3. Вызовите `GET /api/auth/debug-token`
4. Увидите содержимое JWT и список всех участников

### Проверить список участников

Используйте `GET /api/participants` - он не требует авторизации.

## ⚠️ Частые проблемы

1. **"Код недействителен"**
   - Код истёк (действителен 5 минут)
   - Код уже был использован (каждый код можно использовать только 1 раз)
   - Код скопирован неправильно

2. **"401 Unauthorized" при вызове защищённых эндпоинтов**
   - Токен не установлен в Swagger (нажмите "Authorize")
   - Токен истёк (JWT действителен 12 часов)
   - Токен скопирован неправильно

3. **Бот не отвечает**
   - Проверьте, что бот запущен
   - Проверьте переменные окружения
   - Посмотрите логи в терминале, где запущен бот

## 📝 Пример полного flow

```
1. Пользователь → Telegram бот: /login
2. Бот → Пользователь: код "abc123xyz"
3. Пользователь → Swagger: POST /api/auth/bot-code { "code": "abc123xyz" }
4. Сервер → Пользователь: { "access_token": "jwt_token..." }
5. Пользователь → Swagger: GET /api/participants/me (с токеном)
6. Сервер → Пользователь: { "id": "tg_123456", "name": "Иван", ... }
```







