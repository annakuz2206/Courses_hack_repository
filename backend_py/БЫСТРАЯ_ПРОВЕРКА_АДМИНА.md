# ⚡ Быстрая проверка админского функционала

## Шаг 1: Настройте админа

Откройте `config.py` и раскомментируйте строку с вашим Telegram ID:

```python
ADMIN_TELEGRAM_IDS = [5035505042]  # Замените на ваш Telegram User ID
```

Или установите переменную окружения:
```bash
export ADMIN_TELEGRAM_IDS="5035505042"
```

## Шаг 2: Запустите сервер

```bash
cd backend_py
source ../.venv/bin/activate
export JWT_SECRET="test_secret"
python3 main.py
```

## Шаг 3: Получите админский токен

1. Запустите бота (в другом терминале):
```bash
cd backend_py
source ../.venv/bin/activate
export TELEGRAM_BOT_TOKEN="ваш_токен"
python3 telegram_bot.py
```

2. Отправьте `/login` боту в Telegram
3. Скопируйте код
4. В Swagger UI: `POST /api/auth/bot-code` с кодом
5. Скопируйте `access_token` из ответа

## Шаг 4: Проверьте роль

В Swagger UI: `GET /api/auth/debug-token`

Должно быть:
```json
{
  "jwt_payload": {
    "sub": "tg_5035505042",
    "role": "admin",  // ← должно быть "admin"
    ...
  }
}
```

## Шаг 5: Тестируйте админские эндпоинты

### Создание хакатона

**POST** `/api/hackathons`

Body:
```json
{
  "name": "Новый хакатон",
  "description": "Описание",
  "status": "upcoming",
  "maxTeamSize": 5
}
```

**Ожидаемый результат:** 201 Created

### Обновление хакатона

**PUT** `/api/hackathons/{hackathon_id}`

Body:
```json
{
  "description": "Обновлённое описание",
  "status": "active"
}
```

**Ожидаемый результат:** 200 OK

### Удаление хакатона

**DELETE** `/api/hackathons/{hackathon_id}`

**Ожидаемый результат:** 204 No Content

## Автоматический тест

Запустите скрипт:
```bash
cd backend_py
source ../.venv/bin/activate
python3 test_admin.py
```

Скрипт проверит:
- ✅ Публичные эндпоинты
- ✅ Создание хакатона (требует админа)
- ✅ Обновление хакатона (требует админа)
- ✅ Удаление хакатона (требует админа)

## Возможные ошибки

### 403 Forbidden - "Доступ запрещён. Требуются права администратора."

**Причина:** Токен не имеет роли `admin`

**Решение:**
1. Убедитесь, что ваш Telegram ID добавлен в `ADMIN_TELEGRAM_IDS`
2. Получите новый токен через `POST /api/auth/bot-code`
3. Проверьте роль через `GET /api/auth/debug-token`

### 401 Unauthorized

**Причина:** Токен не передан или истёк

**Решение:** Авторизуйтесь заново

## Готово! 🎉

Если все тесты прошли, админский функционал работает корректно!

