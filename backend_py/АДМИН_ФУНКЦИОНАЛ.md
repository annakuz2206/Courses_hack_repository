# 👑 Админский функционал

## Настройка админов

Админы определяются по их Telegram User ID. Чтобы добавить админа:

### Способ 1: Через переменную окружения

Создайте файл `.env` в папке `backend_py` или установите переменную окружения:

```bash
export ADMIN_TELEGRAM_IDS="5035505042,123456789,987654321"
```

Где числа - это Telegram User ID админов через запятую.

### Способ 2: Прямо в коде (для тестирования)

Откройте `config.py` и раскомментируйте строку:

```python
ADMIN_TELEGRAM_IDS = [5035505042]  # Ваш Telegram ID
```

## Как узнать свой Telegram User ID

1. Напишите боту [@userinfobot](https://t.me/userinfobot) в Telegram
2. Он вернёт ваш User ID
3. Добавьте этот ID в `ADMIN_TELEGRAM_IDS`

## Админские эндпоинты

Все админские эндпоинты требуют JWT токен с ролью `admin` в заголовке `Authorization: Bearer <token>`.

### 1. Создание хакатона

**POST** `/api/hackathons`

**Требуется:** Авторизация с ролью `admin`

**Body:**
```json
{
  "name": "Новый хакатон 2024",
  "description": "Описание хакатона",
  "startDate": "2024-06-01T10:00:00",
  "endDate": "2024-06-03T18:00:00",
  "location": "Москва",
  "maxTeamSize": 5,
  "status": "upcoming"
}
```

**Ответ:**
```json
{
  "hackathon": {
    "id": "h5",
    "name": "Новый хакатон 2024",
    "description": "Описание хакатона",
    "startDate": "2024-06-01T10:00:00",
    "endDate": "2024-06-03T18:00:00",
    "location": "Москва",
    "maxTeamSize": 5,
    "status": "upcoming"
  }
}
```

### 2. Редактирование хакатона

**PUT** `/api/hackathons/{hackathon_id}`

**Требуется:** Авторизация с ролью `admin`

**Body (все поля опциональны):**
```json
{
  "name": "Обновлённое название",
  "description": "Новое описание",
  "status": "active",
  "maxTeamSize": 6
}
```

### 3. Удаление хакатона

**DELETE** `/api/hackathons/{hackathon_id}`

**Требуется:** Авторизация с ролью `admin`

**Ответ:** 204 No Content

## Как получить админский токен

1. Убедитесь, что ваш Telegram User ID добавлен в `ADMIN_TELEGRAM_IDS`
2. Получите код от бота командой `/login`
3. Авторизуйтесь через `POST /api/auth/bot-code`
4. Если вы админ, токен будет содержать `"role": "admin"`

## Проверка прав админа

Токен автоматически получает роль `admin`, если ваш Telegram User ID есть в списке `ADMIN_TELEGRAM_IDS`.

Проверить свою роль можно через:
- `GET /api/auth/debug-token` - покажет содержимое токена, включая роль

## Пример использования в Swagger

1. Получите код от бота (`/login`)
2. Авторизуйтесь через `POST /api/auth/bot-code`
3. Нажмите "Authorize" в Swagger UI
4. Вставьте токен
5. Теперь можете вызывать админские эндпоинты:
   - `POST /api/hackathons` - создать хакатон
   - `PUT /api/hackathons/{id}` - обновить хакатон
   - `DELETE /api/hackathons/{id}` - удалить хакатон

## Ошибки

### 403 Forbidden - "Доступ запрещён. Требуются права администратора."

**Причина:** Ваш токен не имеет роли `admin`

**Решение:**
1. Убедитесь, что ваш Telegram User ID добавлен в `ADMIN_TELEGRAM_IDS`
2. Получите новый токен через `POST /api/auth/bot-code`
3. Проверьте роль через `GET /api/auth/debug-token`

### 401 Unauthorized

**Причина:** Токен не передан или истёк

**Решение:** Авторизуйтесь заново

## Безопасность

⚠️ **Важно для production:**
- Храните список админов в переменных окружения или БД
- Не коммитьте `.env` файлы с реальными ID в git
- Регулярно проверяйте список админов
- Используйте более сложную систему ролей для production

