# Integration Guide - ITAM Hackathon Platform

## 🚀 Быстрый старт

### 1. Запуск Backend (FastAPI)

```bash
cd itam_hack
python main.py
```

Backend будет доступен на `http://localhost:8000`
- API Docs: `http://localhost:8000/docs`
- Health check: `http://localhost:8000/health`

### 2. Запуск Frontend (React + Vite)

```bash
cd frontendik
npm install
npm run dev
```

Frontend будет доступен на `http://localhost:5173`

---

## 👤 Система ролей и доступа

### Роли пользователей

1. **user** (по умолчанию) - обычный участник
2. **organizer** - организатор хакатона
3. **super_admin** - главный администратор платформы

### Super Admin

- **ID**: `super_admin_1`
- **Для входа**: используйте dev-login с `participantId: "super_admin_1"`
- **Права**: полный доступ ко всем функциям, включая назначение организаторов

### Тестовые аккаунты

```javascript
// Обычные участники
u1 - Аня (Frontend)
u2 - Илья (Backend)
u3 - Катя (Designer)
u4 - Дима (ML Engineer)

// Супер-админ
super_admin_1 - Super Admin
```

---

## 🔐 Авторизация

### Dev-режим (для разработки)

```javascript
// Frontend
import { authAPI } from './services/api';

// Вход как обычный участник
await authAPI.devLogin('u1');

// Вход как супер-админ
await authAPI.devLogin('super_admin_1');
```

### Production (Telegram)

```javascript
// После получения данных от Telegram Login Widget
await authAPI.telegramAuth({
    id: 123456789,
    first_name: "Иван",
    username: "ivan_dev",
    auth_date: 1234567890,
    hash: "..."
});
```

---

## 📊 Админ-панель

### Доступ

1. Войдите как `super_admin_1` или пользователь с ролью `organizer`
2. Перейдите в Settings (⚙️)
3. Нажмите кнопку "🔧 Админ-панель"
4. Или напрямую: `http://localhost:5173/admin`

### Функционал

#### 1. Управление хакатонами ✅
- Создание новых хакатонов
- Редактирование существующих
- Архивирование
- Настройка дат и размера команд

#### 2. Таблица участников (в разработке)
- Просмотр всех участников
- Фильтрация по статусу (с командой / без команды)
- Экспорт в CSV

#### 3. Таблица команд (в разработке)
- Список всех команд
- Информация о составе
- Экспорт в CSV

#### 4. God Mode (в разработке)
- Ручное добавление участников в команды
- Drag-n-drop интерфейс
- Уведомления участникам

#### 5. Аналитика ✅
- Общая статистика
- Распределение по ролям
- Размеры команд
- Автоматические рекомендации

#### 6. Управление правами (только super_admin)
- Назначение организаторов
- Управление доступом

---

## 🔧 API Endpoints

### Публичные

```
POST /api/auth/telegram - Telegram авторизация
POST /api/auth/dev-login - Dev-логин
GET  /api/participants - Список участников
GET  /api/teams - Список команд
POST /api/swipe - Свайп
GET  /api/matches - Матчи
```

### Админские (требуют роль organizer/super_admin)

```
GET  /api/admin/hackathons - Список хакатонов
POST /api/admin/hackathons - Создать хакатон
PUT  /api/admin/hackathons/{id} - Обновить хакатон
DELETE /api/admin/hackathons/{id} - Архивировать

GET  /api/admin/participants/stats - Статистика участников
GET  /api/admin/analytics - Аналитика
POST /api/admin/add-to-team - Добавить в команду (God Mode)

GET  /api/admin/export/participants - Экспорт участников
GET  /api/admin/export/teams - Экспорт команд
```

### Super Admin только

```
POST /api/admin/set-organizer - Назначить организатора
```

---

## 📝 Примеры использования

### Назначить организатора (Super Admin)

```javascript
import { adminAPI } from './services/api';

// Войдите как super_admin_1
await authAPI.devLogin('super_admin_1');

// Назначьте u1 организатором
await adminAPI.setOrganizer('u1');

// Теперь u1 может войти и увидит кнопку админ-панели
```

### Создать хакатон

```javascript
await adminAPI.createHackathon({
    name: "AI Hackathon 2025",
    description: "Хакатон по искусственному интеллекту",
    startDate: "2025-03-15",
    endDate: "2025-03-17",
    registrationDeadline: "2025-03-14",
    maxTeamSize: 4
});
```

### Получить аналитику

```javascript
const analytics = await adminAPI.getAnalytics('h1');
console.log(analytics);
// {
//   totalParticipants: 50,
//   totalTeams: 12,
//   completeTeams: 8,
//   incompleteTeams: 4,
//   participantsWithoutTeam: 10,
//   roleDistribution: { Frontend: 15, Backend: 20, ... },
//   teamSizeDistribution: { "3": 4, "4": 5, "5": 3 }
// }
```

### Добавить участника в команду вручную (God Mode)

```javascript
await adminAPI.addToTeam('u1', 't1');
// Участник u1 добавлен в команду t1
```

---

## 🎨 User Flow

### Участник

1. **Вход** → Telegram авторизация
2. **Анкета** → Заполнение профиля (если новый)
3. **Выбор хакатона** → Список доступных событий
4. **Выбор стратегии**:
   - "Я один, ищу команду" → Свайп команд
   - "Я создаю команду" → Создание + поиск участников
5. **Свайп** → Лайки команд/участников
6. **Матчи** → Общение в чате

### Организатор

1. **Вход** → Как обычный участник
2. **Settings** → Кнопка "Админ-панель"
3. **Админка**:
   - Создание хакатонов
   - Мониторинг участников и команд
   - Аналитика
   - Ручное распределение (God Mode)
   - Экспорт данных

---

## 🔒 Безопасность

### Важно!

- ❌ Нельзя самоназначиться организатором
- ✅ Только Super Admin может назначать организаторов
- ✅ JWT токены с ролями
- ✅ Проверка прав на каждом эндпоинте
- ✅ Telegram подпись проверяется (в production)

### Настройка для Production

```bash
# Backend (.env)
TELEGRAM_BOT_TOKEN=your_bot_token_here
JWT_SECRET=your_secret_key_here
```

---

## 📦 Структура проекта

```
itam_hack/
├── main.py              # FastAPI backend
├── requirements.txt
└── render.yaml

frontendik/
├── src/
│   ├── components/
│   │   ├── AdminPanel.jsx       # Главная админка
│   │   ├── admin/
│   │   │   ├── HackathonsTab.jsx    # Управление хакатонами
│   │   │   ├── AnalyticsTab.jsx     # Аналитика
│   │   │   └── ...
│   │   ├── AuthForm.jsx
│   │   ├── Feed.jsx
│   │   └── ...
│   └── services/
│       └── api.js           # API клиент
└── package.json
```

---

## 🐛 Troubleshooting

### Backend не запускается

```bash
# Установите зависимости
pip install -r requirements.txt

# Проверьте порт
lsof -i :8000
```

### Frontend не видит API

Проверьте `frontendik/src/services/api.js`:
```javascript
const API_BASE_URL = 'http://localhost:8000';
```

### Нет доступа к админ-панели

1. Проверьте роль: `GET /api/participants/me`
2. Войдите как `super_admin_1`
3. Назначьте себя организатором через API

---

## 📚 Дополнительно

### Swagger UI

Полная документация API: `http://localhost:8000/docs`

### Roadmap

- [x] Система ролей (user/organizer/super_admin)
- [x] Управление хакатонами
- [x] Аналитика с рекомендациями
- [ ] Таблицы участников и команд
- [ ] God Mode с drag-n-drop
- [ ] Уведомления в реальном времени
- [ ] Экспорт в CSV/Excel
- [ ] Telegram бот интеграция
- [ ] Email уведомления

---

**Готово!** Теперь у вас есть полноценная админ-панель для управления хакатонами 🎉
